<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright (C) 2026 Nethesis S.r.l.
  SPDX-License-Identifier: GPL-3.0-or-later

  UAC scenario: call with RTP media playback via PCAP file.
  Validates rtpengine media pass-through and RTP forwarding.

  Usage: sipp [proxy_ip]:5060 -sf uac_call_with_media.xml -s [called_user] -m 1 \
         -d 3000 -mi [local_ip] -mp 30010
  Note: uses SIPp's built-in RTP echo mode if -rtp_echo is set on UAS side.
-->
<scenario name="UAC Call With Media">

  <!-- Send INVITE with SDP -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: "SIPp Media" <sip:sipp-media@[local_ip]:[local_port]>;tag=[pid]SIPpTag0[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:sipp-media@[local_ip]:[local_port];transport=[transport]>
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 1 1 IN IP[local_ip_type] [local_ip]
      s=SIPp Media Call
      c=IN IP[local_ip_type] [local_ip]
      t=0 0
      m=audio [auto_media_port] RTP/AVP 8 0
      a=rtpmap:8 PCMA/8000
      a=rtpmap:0 PCMU/8000
      a=ptime:20
      a=sendrecv

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>

  <!-- Expect 200 OK with SDP (contains rtpengine's rewritten media address) -->
  <recv response="200" rtd="true" crlf="true">
    <action>
      <ereg regexp="(.*)" search_in="hdr" header="To:" assign_to="1"/>
    </action>
  </recv>

  <!-- Send ACK -->
  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: "SIPp Media" <sip:sipp-media@[local_ip]:[local_port]>;tag=[pid]SIPpTag0[call_number]
      [last_To:]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Content-Length: 0

    ]]>
  </send>

  <!-- Send RTP packets (silence pattern for PCMA codec) during the call -->
  <nop>
    <action>
      <exec rtp_stream="8,20,160"/>
    </action>
  </nop>

  <!-- Hold the call while sending RTP -->
  <pause milliseconds="[$d]"/>

  <!-- Stop RTP -->
  <nop>
    <action>
      <exec rtp_stream="pause"/>
    </action>
  </nop>

  <!-- Send BYE -->
  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: "SIPp Media" <sip:sipp-media@[local_ip]:[local_port]>;tag=[pid]SIPpTag0[call_number]
      [last_To:]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" crlf="true"/>

  <ResponseTimeRepartition value="50, 100, 200, 500, 1000, 2000, 5000"/>
  <CallLengthRepartition value="1000, 5000, 10000, 30000, 60000"/>

</scenario>
