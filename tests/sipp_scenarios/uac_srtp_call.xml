<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright (C) 2026 Nethesis S.r.l.
  SPDX-License-Identifier: GPL-3.0-or-later

  UAC scenario: INVITE with SRTP (RTP/SAVP) SDP offer.
  Validates rtpengine SRTP-to-RTP transcoding when proxying to Asterisk.

  Usage: sipp [proxy_ip]:5060 -sf uac_srtp_call.xml -s [called_user] -m 1 -d 3000
-->
<scenario name="UAC SRTP Call">

  <!-- Send INVITE with SRTP SDP -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: "SIPp SRTP" <sip:sipp-srtp@[local_ip]:[local_port]>;tag=[pid]SIPpTag0[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:sipp-srtp@[local_ip]:[local_port];transport=[transport]>
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 1 1 IN IP4 [local_ip]
      s=SIPp SRTP Test
      c=IN IP4 [local_ip]
      t=0 0
      m=audio [auto_media_port] RTP/SAVP 8 0
      a=rtpmap:8 PCMA/8000
      a=rtpmap:0 PCMU/8000
      a=crypto:1 AES_CM_128_HMAC_SHA1_80 inline:dGVzdGtleXRlc3RrZXl0ZXN0a2V5dGVzdGtleTE=
      a=ptime:20
      a=sendrecv

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>

  <!-- Expect 200 OK — SDP should be rewritten by rtpengine -->
  <recv response="200" rtd="true" crlf="true">
    <action>
      <ereg regexp="(.*)" search_in="hdr" header="To:" assign_to="1"/>
    </action>
  </recv>

  <!-- Send ACK -->
  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: "SIPp SRTP" <sip:sipp-srtp@[local_ip]:[local_port]>;tag=[pid]SIPpTag0[call_number]
      [last_To:]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Content-Length: 0

    ]]>
  </send>

  <pause milliseconds="[$d]"/>

  <!-- Send BYE -->
  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: "SIPp SRTP" <sip:sipp-srtp@[local_ip]:[local_port]>;tag=[pid]SIPpTag0[call_number]
      [last_To:]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" crlf="true"/>

  <ResponseTimeRepartition value="50, 100, 200, 500, 1000, 2000, 5000"/>
  <CallLengthRepartition value="1000, 5000, 10000, 30000, 60000"/>

</scenario>
