<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright (C) 2026 Nethesis S.r.l.
  SPDX-License-Identifier: GPL-3.0-or-later

  UAC scenario: basic call (INVITE -> 200 OK -> ACK -> pause -> BYE).
  Validates SIP signaling through the Kamailio proxy.

  Usage: sipp [proxy_ip]:5060 -sf uac_basic_call.xml -s [called_user] -m 1 -d 3000
  -d controls call hold duration in ms (default 3000)
-->
<scenario name="UAC Basic Call">

  <!-- Send INVITE -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] 127.0.0.1:[local_port];branch=[branch]
      Max-Forwards: 70
      From: "SIPp Caller" <sip:sipp@127.0.0.1:[local_port]>;tag=[pid]SIPpTag0[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:sipp@127.0.0.1:[local_port];transport=[transport]>
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 1 1 IN IP4 127.0.0.1
      s=SIPp UAC
      c=IN IP4 127.0.0.1
      t=0 0
      m=audio [auto_media_port] RTP/AVP 8 0
      a=rtpmap:8 PCMA/8000
      a=rtpmap:0 PCMU/8000
      a=ptime:20
      a=sendrecv

    ]]>
  </send>

  <!-- Expect 100 Trying (optional) -->
  <recv response="100" optional="true"/>

  <!-- Expect 180 Ringing (optional) -->
  <recv response="180" optional="true"/>

  <!-- Expect 200 OK -->
  <recv response="200" rtd="true" crlf="true">
    <action>
      <ereg regexp="(.*)" search_in="hdr" header="To:" assign_to="1"/>
    </action>
  </recv>

  <!-- Send ACK -->
  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] 127.0.0.1:[local_port];branch=[branch]
      Max-Forwards: 70
      From: "SIPp Caller" <sip:sipp@127.0.0.1:[local_port]>;tag=[pid]SIPpTag0[call_number]
      [last_To:]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Content-Length: 0

    ]]>
  </send>

  <!-- Hold the call -->
  <pause milliseconds="[$d]" />

  <!-- Send BYE -->
  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] 127.0.0.1:[local_port];branch=[branch]
      Max-Forwards: 70
      From: "SIPp Caller" <sip:sipp@127.0.0.1:[local_port]>;tag=[pid]SIPpTag0[call_number]
      [last_To:]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Content-Length: 0

    ]]>
  </send>

  <!-- Expect 200 OK for BYE -->
  <recv response="200" crlf="true"/>

  <ResponseTimeRepartition value="50, 100, 200, 500, 1000, 2000, 5000"/>
  <CallLengthRepartition value="1000, 5000, 10000, 30000, 60000"/>

</scenario>
